import array as arr
arr.array('d',[1,2.5])
numeros = arr.array('d',[1,2.5]).tolist()
print(numeros)

import numpy as np

##Para arrays se usa numpy, no array
numero = np.array([1,2.5])
print(numero)


